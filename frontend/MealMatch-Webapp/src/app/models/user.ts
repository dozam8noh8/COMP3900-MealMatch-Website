export interface User {
    user_id: number,
    name: String,
    password: String,
    email: String,
    token: String,
}

export interface UserDetails {
    id: number,
    username: string,
    email: string,
}